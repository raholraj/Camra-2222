package com.google.jetpackcamera.core.camera

// Minimal enum to satisfy compilation. Adjust values if the real code expects different members.
enum class DualRecordingMode {
    NONE,
    AUDIO_ONLY,
    VIDEO_ONLY,
    BOTH
}
